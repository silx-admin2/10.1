# Coffee Tracker

Simple single-page coffee tracker using HTML/CSS/JS. Data is stored locally in `localStorage`.

Usage
- Open [index.html](index.html#L1) in a browser.
- Add entries (date, type, cups, notes). Entries persist in your browser.

Optional: serve with a static server

```bash
python -m http.server 8000
# then open http://localhost:8000
```

Want features? I can add export/import, charts, or filtering.
