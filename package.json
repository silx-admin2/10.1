{
  "name": "coffee-tracker",
  "version": "1.0.0",
  "description": "Simple single-page coffee tracker using HTML/CSS/JS",
  "private": true,
  "scripts": {
    "test": "jest --coverage",
    "lint": "eslint app.js"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "coverageThreshold": {
      "global": {
        "lines": 80,
        "functions": 80
      }
    }
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
